# 点球大战代码调试说明

本文记录当前代码中“点球主罚/守门”的关键流程、可调参数，以及如何让机器人固定用右脚向球门左侧踢球、避开中间固定挡板。

## 1. 当前主罚代码位置

主罚核心文件：

- [PenaltyTaker.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Options/PenaltyShootout/PenaltyTaker.cpp)

辅助踢球技能：

- [PenaltyStrikerGoToBallAndKick.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/PenaltyStrikerGoToBallAndKick.cpp)
- [Ball.h](Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/Ball.h)

点球大战总入口：

- [HandlePenaltyShootout.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Options/HandlePenaltyShootout.cpp)

手动按钮进入点球模式：

- [GameStateProvider.cpp](Src/Modules/Infrastructure/GameStateProvider/GameStateProvider.cpp)

## 2. 当前点球主罚流程

当前 [PenaltyTaker.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Options/PenaltyShootout/PenaltyTaker.cpp) 的主罚逻辑是：

1. 进入点球主罚后，先站立并左右看球门，等待约 3 秒。
2. 之后固定进入 `goToBallAndKickLeft`。
3. 目标点固定为对方球门左门柱方向。
4. 固定使用右脚动作 `KickInfo::forwardFastRightPass`。
5. 使用 `kickLength = 2200.f` 控制踢球力度。
6. 如果短时间内看不到球，会先走回点球点后方，再重新找球和踢球。

当前关键代码如下：

```cpp
const Vector2f goalPostOnField(theFieldDimensions.xPosOpponentGoalPost, theFieldDimensions.yPosLeftGoal);
const Vector2f ballPositionOnField = theRobotPose * theBallModel.estimate.position;
const Angle angle = (goalPostOnField - ballPositionOnField).angle() -
                    theBehaviorParameters.penaltyStrikerAngleToLeftPostOffset;

const KickInfo::KickType kickType = KickInfo::forwardFastRightPass;
const float kickLength = 2200.f;
```

## 3. 当前已经固定右脚向左踢

现在已经固定为：

- 固定踢球方向：球门左侧。
- 固定踢球脚：右脚。
- 固定踢球动作：`forwardFastRightPass`。

判断依据：

```cpp
const Vector2f goalPostOnField(theFieldDimensions.xPosOpponentGoalPost, theFieldDimensions.yPosLeftGoal);
const KickInfo::KickType kickType = KickInfo::forwardFastRightPass;
```

`forwardFastRightPass` 在 [Default/kickInfo.cfg](Config/Scenarios/Default/kickInfo.cfg) 中对应：

```cpp
kickLeg = right;
```

所以当前代码不会再根据障碍物选择左/右，也不会再随机选择方向，而是固定右脚向左侧球门踢。

## 4. 角度怎么调

角度参数在这些文件中：

- [Default/behaviorParameters.cfg](Config/Scenarios/Default/behaviorParameters.cfg)
- [Default/K1/behaviorParameters.cfg](Config/Scenarios/Default/K1/behaviorParameters.cfg)
- [Default/T1/behaviorParameters.cfg](Config/Scenarios/Default/T1/behaviorParameters.cfg)

相关参数：

```cpp
penaltyStrikerAngleToLeftPostOffset = 5deg;
```

或 K1/T1 中当前类似：

```cpp
penaltyStrikerAngleToLeftPostOffset = -3deg;
```

当前角度计算方式是：

```cpp
angle = leftPostAngle - penaltyStrikerAngleToLeftPostOffset;
```

这个参数的含义很重要：

- 数值越大，射门越靠近球门中间。
- 数值越小，射门越偏向左门柱方向。
- 如果改成负数，射门会比左门柱方向更偏左一点。

如果球会撞到中间 0.6m 固定挡板，想让球更往左走，应当把 `penaltyStrikerAngleToLeftPostOffset` 调小，而不是调大。

推荐调参顺序：

1. 先从 `5deg` 改到 `0deg`。
2. 如果还撞挡板，再改到 `-3deg`。
3. 如果仍不够偏左，再试 `-5deg` 或 `-8deg`。
4. 不建议一次改太大，否则可能踢出左门柱外。

例子：

```cpp
penaltyStrikerAngleToLeftPostOffset = -5deg;
```

## 5. 力度怎么调

当前力度集中在 [PenaltyTaker.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Options/PenaltyShootout/PenaltyTaker.cpp) 里这一行：

```cpp
const float kickLength = 2200.f;
```

调法：

- 想让球更弱：减小 `kickLength`。
- 想让球更强：增大 `kickLength`。

推荐测试值：

```cpp
const float kickLength = 1800.f; // 更弱
const float kickLength = 2200.f; // 当前值
const float kickLength = 2600.f; // 更强
```

当前踢法是 `forwardFastRightPass`，这个动作会通过 `KickLengthConverter` 把 `kickLength` 转换成实际 kick power。

当前 `forwardFastRightPass` 的范围大致是：

```cpp
range = {min = 1600.01; max = 4500;};
```

所以注意：

- `kickLength` 低于约 `1600` 时，会被限制到最小值，继续减小可能变化不明显。
- `kickLength = 2200` 对应的是比较弱的 pass power，大约在 0.17 左右。
- 如果之前用的是 `forwardFastRightLong`，力度可能几乎不受 `kickLength` 影响，因为 long 动作距离基本是固定的。
- 当前已经换成 `forwardFastRightPass`，所以调 `kickLength` 应该能改变力度。

如果现场感觉“力度没变”，优先检查：

1. 改的是不是 [PenaltyTaker.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Options/PenaltyShootout/PenaltyTaker.cpp) 里的 `const float kickLength`。
2. 当前动作是不是仍为 `KickInfo::forwardFastRightPass`。
3. 是否改完后重新编译并部署到机器人。
4. `kickLength` 是否已经低于最小有效范围，导致被夹到最小值。

## 6. 可调参数汇总

### 主罚方向参数

文件：[Default/behaviorParameters.cfg](Config/Scenarios/Default/behaviorParameters.cfg)

```cpp
penaltyStrikerAngleToLeftPostOffset = 5deg;
penaltyStrikerAngleToRightPostOffset = 10deg;
penaltyStrikerUseObstacles = true;
```

当前主罚代码只走左侧，因此最关键的是：

```cpp
penaltyStrikerAngleToLeftPostOffset
```

`penaltyStrikerAngleToRightPostOffset` 目前基本不会影响主罚，因为右侧踢法状态已经不用了。

`penaltyStrikerUseObstacles` 现在也基本不会影响主罚方向，因为当前代码已经取消了根据障碍物自动选择左/右，改成固定向左踢。

### 主罚动作参数

文件：[PenaltyTaker.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Options/PenaltyShootout/PenaltyTaker.cpp)

```cpp
const KickInfo::KickType kickType = KickInfo::forwardFastRightPass;
const float kickLength = 2200.f;
```

可调含义：

- `kickType`：决定用哪种踢球动作、哪只脚、动作类型。
- `kickLength`：决定期望球滚多远，也就是当前最主要的力度调节入口。

当前建议保持：

```cpp
const KickInfo::KickType kickType = KickInfo::forwardFastRightPass;
```

因为它是右脚，并且比 `forwardFastRightLong` 更容易调小力度。

### 走到球前的准备参数

文件：[PenaltyStrikerGoToBallAndKick.cpp](Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/PenaltyStrikerGoToBallAndKick.cpp)

```cpp
prepareKickDistance = 200.f;
prepareKickDuration = 4000;
minTimeForKick = 10000;
```

含义：

- `prepareKickDistance`：距离踢球位多近时进入准备阶段。
- `prepareKickDuration`：准备阶段等待多久。
- `minTimeForKick`：比赛剩余时间不足时跳过准备，直接踢。

这些一般不用优先调。优先调角度和 `kickLength`。



## 7. 手动进入点球大战模式

在无 GameController 的按钮模式下，代码逻辑在 [GameStateProvider.cpp](Src/Modules/Infrastructure/GameStateProvider/GameStateProvider.cpp)。

右脚右侧 bumper 在 `initial + active` 状态下循环：

1. 普通比赛模式。
2. 点球主罚：语音 `Penalty striker`。
3. 点球守门：语音 `Penalty keeper`。
4. 回到普通比赛：语音 `Soccer`。

胸部按钮用于在 active 和 penalizedManual 之间切换；在点球大战阶段，从 penalizedManual 切回 active 时，会开始对应的 penalty shot。

## 8. 推荐现场调试流程

如果目标是避开球门中间 0.6m 固定挡板，建议按这个顺序调：

1. 保持右脚动作：`forwardFastRightPass`。
2. 先调方向：把 `penaltyStrikerAngleToLeftPostOffset` 从当前值逐步调小。
3. 如果球方向已经绕开挡板，但力度太大或太小，再调 `kickLength`。
4. 每次只改一个参数，编译部署后试踢 3 到 5 次。
5. 如果球撞中间挡板，优先继续减小 `penaltyStrikerAngleToLeftPostOffset`。
6. 如果球方向对但过猛，减小 `kickLength`，例如 `2200 -> 2000 -> 1800`。
7. 如果球方向对但到不了球门，增大 `kickLength`，例如 `2200 -> 2400 -> 2600`。

## 9. 当前推荐起始值

如果当前目标是右脚向左侧避开中间挡板，可以先用：

```cpp
const KickInfo::KickType kickType = KickInfo::forwardFastRightPass;
const float kickLength = 2200.f;
```

角度可以先试：

```cpp
penaltyStrikerAngleToLeftPostOffset = 5deg;
```

如果还是撞中间挡板，再试：

```cpp
penaltyStrikerAngleToLeftPostOffset = 0deg;
```
```cpp
penaltyStrikerAngleToLeftPostOffset = -5deg;
```